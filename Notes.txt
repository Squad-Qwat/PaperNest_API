adding file that needs to be ignored in .gitignore

Adding dependencies in order to make the CLI (View console project) and testing (UnitTesting) works

git pull issue has been resolved, must recreate the project and
re-inserted the directory and the file myself.

Create new class ReviewState.cs to adheres to state-based construction
technique, refactor both ReviewState.cs and Review.cs so LecturerView.cs
and StudentView.cs can interact with it.

Create new class DocumentService.cs to handle uncommited (local save) document
progress in an application, modify the Review.cs and DocumentBody.cs so
the DocumentService.cs works, only resolved the null warning issue in
the Document.cs

Fixed the class ReviewUtil.cs, primarily in the processReview
method.

add the solution as things to be ignored in .gitignore.

Somehow, i forgot to save Console App view Project, fixed now.

Fix the DocumentService, following the new update, might need to fix the
documentRepository.cs and UserService.cs after this.

Internet issues resolved, refactor much of this codebase due to C# class
inside model directory used to mimic SQL table, so had to make another constructor
in classes inside repository directory.

Rest of the changes is according to changes from dev-abiyyu branch, add
the changes from dev-abiyyu branch to fix the class in UniTesting
project, refactor a bit inside LoginRequest.cs to accept username from
Login method in GlobalView.cs, refactor a bit of UserService.cs
functions so it handle AuthController.cs and GlobalView.cs separately.

Add Citation class (Model, repository, service, util, and controller) to
be used by API, and testing the citation service class in the
UnitTesting project.

Updated the Citation part so it might be a litle specific, create the
fix version so it truly reflects the table-driven method (Question: Is
delegate data type and class basically C# template for method?), this Citation
part is also use to reflect the stair-step table-driven contruction,
update the comments on certain parts of the code for context clarity.
Not much change.

Erase the CitationUtilFix.cs since it's been add to staging branch and
merged to the actual branch it belongs.

Refactor the code so it uses if-else inversion technique to enhance
readability (View classes hasn't been refactored that much the day the
last commit is submitted to GitHub), refactor the view classes to
enchance readability, update the same view classes in order to manage citations.

Refactor much of the view classes inspired by the dev branch, for
testing some part of the code do need to be commented out first, update
the login part in GlobalView.cs so it only accept username, uncommented
the regex checking as now the login method only use username and
password for validation checking, reducing potential headache if the
login doesn't work due to input username instead of email and due to
Regex strict checking inside the validation method.

Create DataBindingRepository directory to fetch data from DataBinding
directory.

Move the helpers to each subdirectory as the first identifier, rename
the stateMachine to include Utils too, since the Utils won't be moved to
helpers.

Refactor the AuthController.cs with adding new files from dev-abiyyu
branch, change the direction of the login method here to different login
method inside UserService.cs, also to fix MSTest project as it cause the error (From some
reason, the only error left in this code is Metadata error, where a part
of file required by MSTest project UnitTesting.csproj doesn't exists
inside ASPNetCore Web Project API.csproj).

Fix: There was a dependency error in Citation unit test class.

Updated the view classes in the Console App project View from dev
branch, refactor the LecturerView.cs and StudentView.cs in order to
interact with ReviewState.cs through ReviewUtil.cs (for private use in
this branch, DO NOT pull this branch for any reason).

Forgot to mention: I refactored the review and user workspace respository classes along with it's service classes to add update in the respective controller classes, and i end up have to refactor 2 functions in the Citation Service class, CreateCitation() and UpdateCitation(), to the point of refactor much of it's unit test class too.

Separate much of the clss which implemented ReviewState interface into
different files to avoid anyone read the actual implementation.

Separate the AuthStateMAchine class and Transition class into different
file to enchance readability and avoid circular dependency.

Rename console app solution to add windows GUI view, add Windows GUI
view, not perfect, must be tried tomorrow.

stage-to-dev branch (now obsolete):
Add revision to dev, need to integrate a thing or two from dev-caldera branch first before doing pull request.

Integrate some changes that only exists in dev-caldera, much of it's
content doesn't differ too much from dev-abiyyu, ready for pull request.

dev branch:
Pull request is completed, the message above is unrelated.

Separate the ModelLocalization.cs from Localization.cs from Console App
view project by putting it inside Helper folder inside WEB core API project.

Refactor all the view classes (Pages directory) in order to increase
readability.

Separate the transition class from AuthStateMachine class so the
AuthStateMachine only have and focus on things exclusive to itself.

Minor fix, particularly in null checks and init simpliication.

Another minor fix #1, For some reason, the CitationService.cs do listen to
the CitationServiceTest.cs unit test function by reducing SO many
argument it had before, had to erase all of the existing null arguments
before.

Another minor fix #2, minor null warning resolve and indentation usage.

Another minor fix #3, fix null checking coding mistake that prevents the
view of existing document, where it should return when THERE's no
document.

Another minor fix #4, due to the review logic in the document review, it
is advisable that the code related to this review from lecturer
commented out first before testing this part of the code.

Refactor GlobalViews.cs to limit the range of the validation process, and... Take care of some logic
Refactor LecturerView.cs and StudentView.cs to increase code readability  and some logic a little